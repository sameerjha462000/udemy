SELECT cat_id AS id,name FROM cats;

SELECT name AS 'cat name', breed AS 'kitty breed' FROM cats;

-- NOTE := The keyword AS is used for aliasing. Though this keyword is optional and we can also work around without using it.

-- But then the question arises why to use it if it is optional ?

-- For answer you might have a look at the given link https://stackoverflow.com/questions/42326469/what-is-the-point-using-as-keyword-in-sql-when-aliasing-can-be-done-without-it