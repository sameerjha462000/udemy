1. In video no 208 -- Left join at 9:22 see the table and in my code 208-209. left join.sql see line no 60
   The commands are same but the tables are different in arrangement.
   This shows that in MySQL we do not need to worry in which order the MySQL would put the rows -- it will just pust it.
   