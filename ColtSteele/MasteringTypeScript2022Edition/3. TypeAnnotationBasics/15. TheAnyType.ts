/**
 * `any` is an escape hatch! It turns off the type checking for that variable
 */
/**
 * It sort of defeats the purpose of TS and types, so use it sparingly.
 */

/**
 * It is unique to TypeScript and does NOT exist in JS.
 */

let myData : any = 'Abhishek Jha';
myData = 90;
myData = true;



// ROUGH WORK


