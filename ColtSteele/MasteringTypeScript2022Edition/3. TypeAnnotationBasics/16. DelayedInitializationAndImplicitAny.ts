/**
 * When we do not initialize a variable, its type is set to 'any'
 * To avoid this, if we already know that what type our variable is going to be We can initialize it with it.
 */

let x; // This is of type `any`.

let movie: string;


// ROUGH WORK


