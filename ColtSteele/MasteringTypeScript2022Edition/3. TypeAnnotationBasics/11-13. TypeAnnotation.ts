/**
 * There are types in TypeScript
 */

/**
 * When we compile a .ts file, it automatically gets converted into a .js file irrespective of whether the file had an error or not.
 */

/**
 * To compile a TypeScript file, we type `tsc <filename>` and this would create the corresponding JS file.
 */

let movieTitle: string = 'Amadeus';
// movieTitle = 90; // TypeScript does NOT allow this

/**
 * But we can change movieTitle to another string
 */

movieTitle = 'Arrival'; //  This is valid

/**
 * Slly, we can do with numbers and booleans
 */

let numCatLives: number = 9;
numCatLives++;
// numCatLives = 'Zero'; // This is NOT O.K.


let gameOver: boolean = false;
gameOver = true;

// gameOver = 'true'; // This is INVALID 🥺 



// ROUGH WORK





