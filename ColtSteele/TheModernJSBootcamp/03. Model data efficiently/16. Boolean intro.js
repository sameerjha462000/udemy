/**
 * There are two values in this -- true and false
 */


/**
 * 'true' is different from true(The former is a string and the latter is boolean)
 * 'false' is different from false(The former is a string and the latter is a boolean)
 */

/**
 * Examples of booleans := 
 */

let isLoggedIn = true;
let gameOver = false;
const isWaterWet = true;

let isHungry = true;
// But after eating food
isHungry = false;

/**
 * In JS, variables can change type. This is the feature people hate the most
 * For this reason, we have something called as TypeScript in which you cannot change type of the variable.
 */

let isAngry = false;
isAngry = 1239;